# journals — declined

Per-task journals recording what happened.

**Why not.** agent-hive removed theirs as "write-only artifacts nobody read", replaced by context files. `result.md` plus `status.json` already carry what a later reader needs.
